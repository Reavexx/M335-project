import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm335-project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
